apiVersion: v1
kind: Service
metadata:
  labels:
    app: kib-manual
  name: kibana
  namespace: logging
spec:
  ports:
  - port: 5601
    nodePort: 32001
    protocol: TCP
    targetPort: 5601
  selector:
    app: kib-manual
  type: NodePort
